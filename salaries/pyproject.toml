[project]
name = "salaries"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cactus-commits", email = "aurora.dev@gmai.com" }
]
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.2",
    "streamlit>=1.56.0",
]

[project.scripts]
salaries = "salaries:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
